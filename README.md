# ninja_cat
Ninja to Cat exercise -- used the 'data-alt-src' inside the img tags to create custom attributes, which I used by creating a variable inside the jquery functions to call on those attributes. 
